--包
package 关键字用于声明类所在的包（package），那么其他类需要引用该包的内容就要用import了。

创建package包时，可以直接输入cn.itcast直接创建二级文件夹而无需一级一级创建文件夹。

创建了package包，要用时使用import关键字。

--内部类

内部类就是在一个类里面再创建一个新的类。内部类的使用可以用来避免误修改。

当类的属性加上final后，final在初始化后就不能再改变，只能get而不能set修改。final方法不能被子类重写，final类不能被继承。


