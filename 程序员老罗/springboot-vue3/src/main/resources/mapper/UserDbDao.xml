<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootvue3.dao.UserDbDao">

    <resultMap type="com.example.springbootvue3.entity.UserDb" id="UserDbMap">
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="userPhoneNumber" column="user_phone_number" jdbcType="VARCHAR"/>
        <result property="userPassword" column="user_password" jdbcType="VARCHAR"/>
        <result property="userBirthday" column="user_birthday" jdbcType="TIMESTAMP"/>
        <result property="userGender" column="user_gender" jdbcType="INTEGER"/>
        <result property="userPosition" column="user_position" jdbcType="INTEGER"/>
        <result property="userRole" column="user_role" jdbcType="VARCHAR"/>
        <result property="userCreateTime" column="user_create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserDbMap">
        select
          user_id, user_name, user_phone_number, user_password, user_birthday, user_gender, user_position, user_role, user_create_time
        from user-db
        where user_id = #{userId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserDbMap">
        select
          user_id, user_name, user_phone_number, user_password, user_birthday, user_gender, user_position, user_role, user_create_time
        from user-db
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="userName != null and userName != ''">
                and user_name = #{userName}
            </if>
            <if test="userPhoneNumber != null and userPhoneNumber != ''">
                and user_phone_number = #{userPhoneNumber}
            </if>
            <if test="userPassword != null and userPassword != ''">
                and user_password = #{userPassword}
            </if>
            <if test="userBirthday != null">
                and user_birthday = #{userBirthday}
            </if>
            <if test="userGender != null">
                and user_gender = #{userGender}
            </if>
            <if test="userPosition != null">
                and user_position = #{userPosition}
            </if>
            <if test="userRole != null and userRole != ''">
                and user_role = #{userRole}
            </if>
            <if test="userCreateTime != null">
                and user_create_time = #{userCreateTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from user-db
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="userName != null and userName != ''">
                and user_name = #{userName}
            </if>
            <if test="userPhoneNumber != null and userPhoneNumber != ''">
                and user_phone_number = #{userPhoneNumber}
            </if>
            <if test="userPassword != null and userPassword != ''">
                and user_password = #{userPassword}
            </if>
            <if test="userBirthday != null">
                and user_birthday = #{userBirthday}
            </if>
            <if test="userGender != null">
                and user_gender = #{userGender}
            </if>
            <if test="userPosition != null">
                and user_position = #{userPosition}
            </if>
            <if test="userRole != null and userRole != ''">
                and user_role = #{userRole}
            </if>
            <if test="userCreateTime != null">
                and user_create_time = #{userCreateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="userId" useGeneratedKeys="true">
        insert into user-db(user_name, user_phone_number, user_password, user_birthday, user_gender, user_position, user_role, user_create_time)
        values (#{userName}, #{userPhoneNumber}, #{userPassword}, #{userBirthday}, #{userGender}, #{userPosition}, #{userRole}, #{userCreateTime})
    </insert>

    <insert id="insertBatch" keyProperty="userId" useGeneratedKeys="true">
        insert into user-db(user_name, user_phone_number, user_password, user_birthday, user_gender, user_position, user_role, user_create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.userName}, #{entity.userPhoneNumber}, #{entity.userPassword}, #{entity.userBirthday}, #{entity.userGender}, #{entity.userPosition}, #{entity.userRole}, #{entity.userCreateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="userId" useGeneratedKeys="true">
        insert into user-db(user_name, user_phone_number, user_password, user_birthday, user_gender, user_position, user_role, user_create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userName}, #{entity.userPhoneNumber}, #{entity.userPassword}, #{entity.userBirthday}, #{entity.userGender}, #{entity.userPosition}, #{entity.userRole}, #{entity.userCreateTime})
        </foreach>
        on duplicate key update
        user_name = values(user_name),
        user_phone_number = values(user_phone_number),
        user_password = values(user_password),
        user_birthday = values(user_birthday),
        user_gender = values(user_gender),
        user_position = values(user_position),
        user_role = values(user_role),
        user_create_time = values(user_create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user-db
        <set>
            <if test="userName != null and userName != ''">
                user_name = #{userName},
            </if>
            <if test="userPhoneNumber != null and userPhoneNumber != ''">
                user_phone_number = #{userPhoneNumber},
            </if>
            <if test="userPassword != null and userPassword != ''">
                user_password = #{userPassword},
            </if>
            <if test="userBirthday != null">
                user_birthday = #{userBirthday},
            </if>
            <if test="userGender != null">
                user_gender = #{userGender},
            </if>
            <if test="userPosition != null">
                user_position = #{userPosition},
            </if>
            <if test="userRole != null and userRole != ''">
                user_role = #{userRole},
            </if>
            <if test="userCreateTime != null">
                user_create_time = #{userCreateTime},
            </if>
        </set>
        where user_id = #{userId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from user-db where user_id = #{userId}
    </delete>

</mapper>

